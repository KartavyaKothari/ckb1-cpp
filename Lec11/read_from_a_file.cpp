#include<bits/stdc++.h>
using namespace std;

int main(){
    // fstream fs;
    // fs.open("aman.eren",ios::in);

    // ifstream fin;
    // fin.open("aman.eren");

    ifstream fin("aman.eren");

    string str;

    // fin>>str;
    getline(fin,str);

    cout<<str;

    return 0;
}